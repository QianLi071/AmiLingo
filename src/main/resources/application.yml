server:
  port: 8080
  tomcat:
    max-connections: 65535
    threads:
      max: 1000
    accept-count: 1600

spring:
  application:
    name: platform
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  datasource:
    url: ${DATABASE_HOST:jdbc:mysql://localhost:3306/amilingo}
    username: ${DATABASE_USERNAME:root}
    password: ${DATABASE_PASSWORD:}
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: ${REDIS_HOST}
      password: ${REDIS_PASSWORD}
      port: 6379
      timeout: 5000
      lettuce:
        pool:
          max-active: 16
          min-idle: 0
          max-idle: 4

microservice:
  workerid: 0
  dadacenterid: 0

jwt:
  secret: ${JWT_SECRET:YourSecretKeyForJwtTokenGeneration1234567890}
  expiration: ${JWT_EXPIRATION:86400000}

# Python 离线 AI 服务（OfflineAssistant / FastAPI）地址
# Java 端 PythonAiGateway 通过 HTTP 调用此服务完成写作批改等 AI 推理
ai:
  python:
    base-url: ${AI_PYTHON_BASE_URL:http://localhost:8000}
